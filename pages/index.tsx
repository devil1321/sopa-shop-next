import { GlobalComponents } from '@/components/global'

export default function Home() {
  return (
    <GlobalComponents.LayoutWithFooter className='sign-in' title="Sign In" meta={[]}>
      sign in
    </GlobalComponents.LayoutWithFooter>
  )
}
